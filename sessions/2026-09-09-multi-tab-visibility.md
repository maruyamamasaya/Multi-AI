# Multi-tab visibility

- 独立した `WebContentsView` タブを最大32個保持し、同時表示は1～4個に分離した。
- 非表示は `setVisible(false)` のみとし、URL、履歴、ページ、ログインセッションを破棄・再読込しない。
- タブバーに表示切替と完全クローズを別操作として追加した。
- 同一サービスのタブを連番表示する28pxの表示領域ヘッダーを追加した。
- 表示セットをワークスペースへ保存し、比較候補と共通プロンプト対象を表示中タブへ限定した。
