# ペイン簡易操作バー

## 結果

- 各AI表示領域上部の28pxヘッダーへ、現在URL、個別更新、最小化、完全に閉じる操作を追加した。
- 最小化は既存のタブ表示切替を再利用し、WebContentsと上部タブを維持したまま待機中へ移す。
- 完全に閉じる操作は上部タブの×と同じ削除処理、更新は対象ビューの既存reload IPCを利用する。
- 集中表示・比較モード・最後の表示中タブに対する既存の操作制約を維持した。

## 検証

- ペインごとのURL表示、個別更新、最小化、完全削除をrenderer unit testへ追加した。
