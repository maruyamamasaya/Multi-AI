# 名前付きワークスペース保存

日付: 2026-09-09

## 変更

- 通常の終了時復元とは別に、バージョン付き `named-workspaces.json` へワークスペースを名前付き保存する機能を追加した。
- 保存、同名上書き確認、一覧、切替、削除のUIと限定IPCを追加した。
- 読み込み時はビュー構成を確定してからURLをロードし、読み込んだ構成を通常の `workspace.json` にも反映する。
- 集中表示は保存せず、集中表示中の名前付きワークスペース操作を抑止する。

## 検証

- Fast Validation: 成功（lint、typecheck、unit test 47件）。
- Full Validation: 成功（lint、typecheck、unit test 47件、production build）。
- Electron実動作: 2・3・4画面構成を個別に保存し、2画面と4画面の相互切替でURL、AIサービス、並び順、選択画面を復元した。
- 同名上書き: 確認後も一覧件数が増えず、既存項目が更新された。
- 削除: 3画面の保存を削除し、現在の4画面構成に影響しないことを確認した。
- 再起動確認: 名前付き一覧が残り、通常の `workspace.json` から直前の4画面・Claude選択が通常表示で復元された。
- 検証後は全テスト用保存を削除し、元の2画面・画面1選択へ戻した。
