# 起動時ワークスペース選択

日付: 2026-09-09

## 変更

- 名前付き保存がある場合だけ、「前回の続き」と保存済み構成を1クリックで選べる起動画面を追加した。
- 名前付き項目へ名前、画面数、含まれるAIサービスを表示する。
- 選択完了まで外部WebContentsViewを非表示にし、起動UIとの前面競合を防ぐ。
- 名前付き構成で開始した場合は通常の `workspace.json` にも反映する。
- 壊れたJSONや有効な保存がない場合は空一覧として従来どおり起動する。

## 検証

- Fast Validation: 成功（lint、typecheck、unit test 58件）。
- Full Validation: 成功（lint、typecheck、unit test 58件、production build）。
- 保存なし: 選択画面を省略し、従来の2画面・画面1選択で起動した。
- 選択画面: 名前付き3画面の名前、画面数、Perplexityを表示し、選択中は起動モーダルが表示された。
- 前回の続き: 4画面の順序とClaude選択を復元した。
- 名前付き開始: 3画面の順序とPerplexity選択を復元し、次回起動の「前回の続き」にも同じ構成が反映された。
- 不正データ: unit testで壊れたJSONファイルを空一覧へフォールバックすることを確認した。
- 検証後はテスト用の名前付き保存と追加画面を削除し、再起動で元の2画面・画面1選択と選択画面の省略を確認した。
