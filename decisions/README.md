# 設計判断ログ

システムの将来の保守に影響する重要な設計判断を、1 判断につき 1 ファイルで記録します。現在、記録対象となる技術判断はまだありません。

## ファイル名

`NNN-short-title.md` の形式を使い、`NNN` は `001` からの連番とします。一度記録した判断は消去せず、判断が変わった場合は新しいログから旧ログを参照します。

## 必須項目

各ファイルに次を簡潔に記載します。

- 背景
- 問題
- 検討した選択肢
- 採用した判断
- 採用理由
- デメリット・トレードオフ
- 将来見直す条件

小さな実装の選択は記録対象にしません。
