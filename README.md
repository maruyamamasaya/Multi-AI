# Multi-AI

複数の AI サービスの既存 Web 画面を、一つのウィンドウ内に並べて使うためのデスクトップアプリケーションです。

AI API を共通化するのではなく、ChatGPT、Claude、Gemini など、利用者が契約・利用している Web 版サービスを「一つの作業机」にまとめることを目指します。現在は要件整理を終え、最小版の実装に着手する前の段階です。

## 最初に目指すもの

- 2 分割・4 分割で複数の AI サービスを同時表示する
- 各画面を通常のブラウザと同様に直接操作できる
- ログイン状態、選択したサービス、レイアウトを保持する
- 戻る、進む、再読み込み、ズーム、画面の一時最大化を提供する
- 用途ごとの画面構成をワークスペースとして保存する

初期版では、同一プロンプトの自動一括送信や、各サービスの画面構造に依存する操作自動化は対象外です。

## セットアップ

現時点でセットアップが必要なソースコードや依存関係はありません。Electron、TypeScript、React を使った最小構成の追加後に、実際の手順をここへ追記します。

## 起動方法

起動対象はまだありません。

## 環境変数

必要な環境変数はまだありません。機密情報はリポジトリにコミットせず、将来の `.env.example` には値ではなく変数名と説明のみを記載してください。

## 開発

開発を始める前に [`AGENTS.md`](AGENTS.md) と [`CURRENT.md`](CURRENT.md) を読んでください。システムの現行構成は [`ARCHITECTURE.md`](ARCHITECTURE.md) に記載します。

現時点で lint、typecheck、test、build のコマンドは定義されていません。ツールチェーン導入時に、再現可能なコマンドを本ファイルへ追記してください。

## デプロイ

デプロイ対象とデプロイ方法はまだ定義されていません。
