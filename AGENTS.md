# AI エージェント作業ルール

このリポジトリで作業するすべての AI エージェントは、以下のルールに従ってください。

## 作業開始時

1. 最初に `CURRENT.md` を読み、現在地と次の作業を確認する。
2. 構成や責務に関わる作業では、必要に応じて `ARCHITECTURE.md` を確認する。
3. 過去の設計判断が関係する場合は `decisions/` を確認する。
4. コードだけを見て仕様を推測しない。文書、テスト、過去の判断、利用者の要求を照合する。

## 作業中

- 不明点を勝手に仕様として確定しない。必要な場合は確認し、確認できない場合は仮定を明記する。
- 既存仕様、設計、ユーザーデータを破壊する変更は避ける。互換性や移行方法を検討する。
- 大きな構成変更を行った場合は `ARCHITECTURE.md` を更新する。
- 重要な技術判断を行った場合は、1 判断につき 1 ファイルで `decisions/` に記録する。
- `README.md` には利用者に必要な情報だけを記載し、調査ログや作業履歴を混在させない。

## 検証と作業終了時

1. 変更後は可能な範囲で lint / typecheck / test / build を実行し、実行できないものとその理由も記録する。
2. `sessions/` に作業内容、変更、検証結果、残件を記録する。
3. 実装状況、既知の問題、次の作業など現在状態が変化した場合は `CURRENT.md` を更新する。過去の履歴はここへ蓄積しない。
