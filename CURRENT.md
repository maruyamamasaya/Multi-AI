# 現在のプロジェクト状態

最終更新: 2026-09-09

## 現在のフェーズ

プロダクト方針と最小版の範囲を定義し、実装開始を待つ段階です。

複数の AI サービスの既存 Web 画面を、Electron ベースのデスクトップアプリ内に分割表示します。AI API の統合ではなく、各サービスを通常のブラウザとして直接利用できる作業環境を提供します。

## 実装済み

- プロジェクト名と「複数 AI を扱う基盤」という概要
- 利用者向けの `README.md`
- 現在地、構成、設計判断、作業履歴を分離して管理する文書構造
- タイル型マルチ AI ブラウザというプロダクト方針
- Electron、TypeScript、React、`WebContentsView` を使う初期技術方針
- 最小版の機能範囲と対象外事項

## 進行中

- なし

## 未実装

- アプリケーションコード
- テスト、lint、typecheck、build のツールチェーン
- レイアウトおよびワークスペースのローカル保存
- AI サービスごとのログイン・セッション保持
- サービス別の互換性検証
- デプロイ構成

## 既知の問題

- 実行・検証できる実装はまだありません。
- 各 AI サービスのログイン可否と Electron 上での動作は未検証です。
- 複数画面を同時に動かした場合のメモリ消費は未計測です。

## 次に行うこと

1. Electron、TypeScript、React の最小構成を作成する。
2. `WebContentsView` で 2 つの任意 Web ページを左右に表示する。
3. レイアウト変更、基本ナビゲーション、セッション保持を順に追加する。
4. 対象 AI サービスでログインと基本操作を検証する。
5. lint、typecheck、test、build と起動手順を追加する。
