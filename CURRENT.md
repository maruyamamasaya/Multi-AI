# 現在のプロジェクト状態

最終更新: 2026-09-09

## プロジェクト

複数の AI サービスの既存 Web 画面を一つのデスクトップ画面に並べる、タイル型マルチ AI ブラウザです。

## 現在のフェーズ

1～6画面の構成とURLを再起動後も復元できる、最小ブラウザとして動作する段階です。プロダクト方針は [`decisions/001-tiled-electron-browser.md`](decisions/001-tiled-electron-browser.md) を参照してください。

## 実装済み

- Electron main process、preload、React renderer
- context isolationを維持した最小IPC疎通
- IPC状態を確認できる最小画面
- `WebContentsView`による `example.com` と `example.org` の左右分割表示
- ウィンドウリサイズへの追従、新規ウィンドウとWeb権限要求の拒否
- 管理対象Web画面からの安全なクリップボード書き込み（コピー）のみ許可し、読み取りを含む他のWeb権限は拒否
- 各ビューのURL入力、戻る、進む、再読み込み
- URL・履歴・読み込み状態のmainからrendererへの同期
- `http` / `https` 限定のURL正規化とIPC共有型
- 1～6画面の追加・選択中画面の削除・左右並び替えと、2～6画面を横一列の等幅列にする自動レイアウト
- 選択中画面だけを一時的に表示し、WebContentsを維持したまま元の分割表示へ戻せる集中表示
- 7つの対応AIから選んで新しい画面を追加するAIサービス選択ランチャー
- 各画面タブへのAIサービス名・アイコン表示と、会話URLを含むホスト判定
- 対応AIの会話URLだけをサービス情報付きで保存・表示・削除するローカルブックマーク
- 保存済みAI会話をタイトル検索・サービス絞り込みし、タイトル／URL編集・表示・確認付き削除できる専用管理画面
- 「ファイル」メニューと `Ctrl/Cmd+Shift+B` からAI会話管理を開く導線
- 画面数、各画面の最終URL、選択中画面、分割レイアウトのローカル保存・復元
- 画面構成を任意の名前で保存・上書き・切替・削除できるローカル名前付きワークスペース
- 名前付き保存がある場合に「前回の続き」または保存構成を1クリックで選べる起動選択画面
- 画面ごとに送信先を選び、同じテキストを複数AIへ独立送信する共通プロンプト
- 共通送信したAI画面を均等配置し、対象除外・再追加・比較内集中表示できる回答比較v1
- 各AI表示領域ヘッダーの未実行・実行中・失敗を示す保守的な回答状態表示
- 全AI画面へ50～200%の段階式倍率を一括適用し、新規画面にも引き継ぐ共通Zoom
- 最大32個の独立WebContentsタブを保持し、そのうち1～6タブを表示・待機・再表示できるChrome型タブ管理
- 5～6画面でも縦長カラムを維持し、狭いウィンドウでは各ペインを240px以上に保つ同期横スクロール
- タブごとの「表示中」「待機中」ラベル、完全に閉じる操作との区別、7画面目を表示しようとした際の軽い上限通知
- 同一AIの複数タブを連番で識別できる、各表示領域上部の28pxタブヘッダー
- 各表示領域の28px操作バーに現在URL、個別更新、待機中への最小化、完全に閉じる操作を表示
- 表示領域ヘッダーのドラッグ＆ドロップによるWebContents状態を維持した並び替え
- サービスアイコン、連番、表示状態、×だけの2行折り返し対応コンパクトタブ一覧
- 終了時の保存完了待機と、全ビュー登録後にURLロードを始める安全な復元順序
- 存在しない・壊れた保存データと非HTTP(S) URLの安全な1画面フォールバック
- ESLint、TypeScript、Vitest、Vite build
- Fast / Full Validationの標準入口

## 進行中

- なし

## 未実装

- Cookieを含むブラウザセッション方針と互換性検証
- AIサービスのログイン互換性検証、アプリ配布

## 既知の問題

- 実アプリ画面の自動E2Eとパッケージングは未導入です。
- AIサービス側の回答生成完了を安全に取得する仕組みは未導入のため、送信成功後は誤って完了扱いせず実行中表示を維持します。
- 7サービスの未認証表示はElectron実機で確認済みです。Googleは埋め込みユーザーエージェントでのOAuthを公式に拒否しており、ClaudeのGoogleログインは現行の新規ウィンドウ拒否とも競合します。認証情報入力後のログイン完了は未確認です。

## 技術的負債

- main processのナビゲーションIPCはElectron runtimeでの自動テストがなく、共有URL処理とrenderer操作のみunit test済みです。

## 次に行うこと

1. Google以外のログイン方式を含め、利用者操作で各サービスの認証完了を確認する。
2. Google OAuthを迂回せず成立させられるシステムブラウザ連携方式があるか、サービスごとに判断する。
3. サービスURL変更を追跡する運用方針を決める。
